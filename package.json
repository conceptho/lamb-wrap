{
  "name": "lamb-wrap",
  "version": "0.1.0",
  "description": "Lambda entrypoint wrapper",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test MOCHA_FILE=$CIRCLE_TEST_REPORTS/output.xml mocha --recursive  --reporter $MOCHA_REPORTER"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/conceptho/lamb-wrap.git"
  },
  "author": "Conceptho",
  "license": "MIT",
  "dependencies": {
    "mongoose": "^4.5.3"
  },
  "devDependencies": {
    "mocha": "^2.5.3",
    "mocha-junit-reporter": "^1.11.1",
    "should": "^9.0.2"
  }
}
